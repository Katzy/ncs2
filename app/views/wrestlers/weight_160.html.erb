<div class="container" style="padding-left: 5em;padding-right: 5em;">
  <div class="ui center aligned segment">
<h1>160</h1>
(<%= @count2 %>)
        <p>
          <% if user_signed_in? && current_user.admin? %>
          Download:
            <%= link_to "CSV", wrestlers_weight_160_path(format: "csv") %> <br><br>
            <%= link_to "VIEW ALTERNATES ONLY", wrestlers_alternates_path(weight: @wt) %>
            <% end %>

        </p>
</div>

    <%= render partial: 'wrestlers/index_by_weight', locals: { wrestlers: @wrestlers, user: @user } %>
</div>