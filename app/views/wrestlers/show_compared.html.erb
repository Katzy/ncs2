<div class="ui center aligned segment">
  <h1><%= link_to "#{@weight}", send("wrestlers_weight_#{@weight}_path") %></h1>
  (<%= @count %>)
       
</div>

    <%= render partial: 'wrestlers/index_by_weight', locals: { wrestlers: @w, user: @user } %>
