

  <% if current_user %>

      <div class="container" id="comments">
          <ul class="media-list">
            <%= render @comments %>
          </ul>
        </div>
      </div>

    <div class="ui center aligned segment">
      <div class="header">
        <h1>Join the discussion!</h1>
      </div>

    <%= render '/comments/form' %>
    </div>
  <% else %>
    <h1>Sign in or Create an account to chat with other NCS Wrestling fans.</h1>
  <% end %>
